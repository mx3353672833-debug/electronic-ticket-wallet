# 隐私与安全

这是适合小范围朋友使用的票夹，支持邮箱注册和用户间数据隔离；没有经过独立安全审计，不应视作成熟的大规模多租户平台。

- 只部署自己有权保存的票据；将数据目录放在 Web 根目录之外。
- 生产环境必须 HTTPS。不要关闭 Secure Cookie、Origin 检查或图片认证。
- 服务只监听回环地址。反向代理必须覆盖客户端传入的 `X-Real-IP`，否则登录限速可能失效。
- 登录态在内存中；重启会要求重新登录，密码重置会使旧登录态失效。注册默认需要邀请码和邮箱验证码；不得依靠前端隐藏按钮实施权限控制。
- 普通用户的数据位于独立目录，既有收藏只属于预配置的站长账号。绝不以“第一个注册的人”决定站长或私密收藏的归属。
- 邮箱验证码使用 HMAC 摘要保存，10 分钟有效、仅使用一次、错误 5 次后失效。生产环境没有明文验证码输出或开发后门；SMTP 失败不会开放注册绕过。
- 邮件配置、账号哈希、验证码状态和建议记录均为私密资料。建议提交会把用户主动填写的内容和账号邮箱发送给站长，不附带票据照片或轨迹。
- 定期备份完整数据目录，并把备份放到另一个受控位置；同机快照不能代替异地备份。
- 不要把凭据、真实照片、OCR 文本、精确轨迹、日志或私密导出放进 Issue、PR、截图或 Git 历史。
- 地图瓦片向第三方服务请求；请评估出行区域暴露风险。
- 不加载他人提供的 Python pickle 路网缓存。
- 文件存储需要单个 Node 进程运行；不要开启多进程或多副本写同一数据目录。用户配额和磁盘保留空间不是异地备份。

发现漏洞时，请先使用仓库 Security 页面的私密报告渠道（如已启用）。如果不可用，只发起不包含利用细节或私人数据的联系请求，待建立私密渠道后再提供详情。
